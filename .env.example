MOODLE_BASE_URL=https://teaching.kse.org.ua
MOODLE_USERNAME=your.email@kse.org.ua
MOODLE_PASSWORD=your-moodle-password

# Optional: comma-separated course ids to limit sync to specific courses.
# Leave empty to sync all enrolled courses. Run `python main.py --list` to see ids.
MOODLE_COURSE_IDS=
